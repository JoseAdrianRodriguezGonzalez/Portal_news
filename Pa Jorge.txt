Hola xd

Se modifico:

el docker-compose.dev.yml, ya le agregué los servicios de Mongo, Cassandra y Redis y ya todo corre en una misma red.
______________________________________________________________
Se crearon:

config, donde van los archivos que conectan cada DB (Mongo, Cassandra y Redis)

Cassandra esta hecho 100/100 por IA, la neta no le entendí porque yo falte, entonces ahí le echas una checada de más
(ese script crea automáticamente el Keyspace portal_news y las tablas usuarios y bitacora si no existen)

en models esta noticias y usuarios, donde se definen los metadatos (usuarios no tiene nada, ahí me quede).

________________________________________________________________
Actualmente

Cuando lo levantas, crea las conexiones a los servidores y ya cumplimos con las chambas:

13 - Entorno 100% funcional con las 3 BDs
11 - Las elecciones (que creo que si las hice igual que Adrian porque esto lo hice el viernes y ya no lo 
      cheque xd entonces que yo recuerdo hice lo mismo que lo que el msj de Adrian pedía)
12 - Tablas de Cassandra y esquema de Mongo definidos

y vamos en la 14 (Falta escribir la clase/funciones para crear y buscar usuarios en Cassandra.)


______________________________________________________________
Para levantar:

# 1. Levantar los contenedores
docker-compose -f docker-compose.dev.yml up -d --build

# 2. Instalar las dependencias dentro del contenedor (si no, da error de módulos)
docker exec -it backend_dev npm install mongoose redis cassandra-driver

# 3. Ver logs para confirmar conexión (deben salir 3 mensajes de éxito)
docker logs -f backend_dev


Y debe salir algo así:

Hola mundo fsjdfhshsdksjkh
Servidor corriendo en puerto 3000
MongoDB Conectado
Redis Conectado
Conectado al servidor Cassandra
Keyspace configurado
Tabla: Usuarios
Tabla Bitácora lista
emmvzq@DESKTOP-7IEMKSH:~/pf/Portal_news$


Los errores se guardan gracias a nodemon que usa Adrian, para que lo tengas en cuenta.

Ahí me mandas lo que avances y ahi nos la llevamos